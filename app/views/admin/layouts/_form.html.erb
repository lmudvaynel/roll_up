<%= semantic_form_for [:admin, resource] do |f| %>
  <%= f.inputs do %>
    <%= f.input :name, input_html: {rows: 1} %>
    <%= f.input :content, as: :ckeditor %>
  <% end %>
  
  <%= f.actions %>
<% end %>